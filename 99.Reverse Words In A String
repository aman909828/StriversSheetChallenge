def reverseString(s: str) -> str:
    arr=s.split(" ")
    i=0
    while i<len(arr):
        if arr[i].isalnum()==False:
            arr.pop(i)
        else:
            i+=1
    arr=arr[::-1]
    return " ".join(arr)
